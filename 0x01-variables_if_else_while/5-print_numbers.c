#include<stdio.h>

/**
 *main - single numbers of base 10 starting from 0
 *followed by a new line
 *return : 0 (Success)
 */

int main(0)
{
int a;
a = 0;
while (a < 10)
printf("%d/n", a);
a++;
return (0);
}
