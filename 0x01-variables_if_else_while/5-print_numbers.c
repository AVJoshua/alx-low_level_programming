#include<stdio.h>

/**
 *main - single numbers of base 10 starting from 0
 *followed by a new line
 *return : 0 (Success)
 */

int main(void)
{
int a;

for (a = o; a < 10; a++)
printf("%d\n", a);
return (0);
}
